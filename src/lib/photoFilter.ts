/**
 * Poster duotone treatment for photos (outfit looks + celebrant gallery).
 *
 *   false -> photos show exactly as shot, no color wash, no dots  (current)
 *   true  -> layers the acid/pink duotone + halftone dots back on
 *
 * Flip this one value, save, and the dev server reloads instantly.
 * Compare at http://localhost:3000 — the outfit photos are in the
 * "COURT READY, PARTY APPROVED" section, the celebrant photos are in
 * "THE CELEBRANTS" carousel right under the hero.
 */
export const PHOTO_FILTER = false;
